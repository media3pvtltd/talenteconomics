<%= render 'refinery/admin/search_header', :url => refinery.profiles_admin_profiles_path %>
<div class='pagination_container'>
  <% if @profiles.any? %>
    <%= render 'profiles' %>
  <% else %>
    <p>
      <% unless searching? %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% else %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% end %>
    </p>
  <% end %>
</div>
