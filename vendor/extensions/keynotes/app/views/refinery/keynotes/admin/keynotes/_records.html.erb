<%= render 'refinery/admin/search_header', :url => refinery.keynotes_admin_keynotes_path %>
<div class='pagination_container'>
  <% if @keynotes.any? %>
    <%= render 'keynotes' %>
  <% else %>
    <p>
      <% unless searching? %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% else %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% end %>
    </p>
  <% end %>
</div>
