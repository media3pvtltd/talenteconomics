<% content_for :body do %>
  <ul id="courses">
    <% @courses.each do |course| %>
      <li>
        <%= link_to course.place, refinery.courses_course_path(course) %>
      </li>
    <% end %>
  </ul>
<% end %>

<%= render '/refinery/content_page' %>
